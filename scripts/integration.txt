-- test: just moveq
	moveq	#2, %d0
	moveq	#60, %d1
	moveq	#-1, %d2
1:	jmp	1b
--
	%d0 ==  2 &&
	%d1 == 60 &&
	%d2 == (-1i32 as u32)

-- test: addl (3 + 5)
	moveq	#3, %d0
	moveq	#5, %d1
	addl	%d1, %d0
1:	jmp	1b
--
	%d0 == 8
